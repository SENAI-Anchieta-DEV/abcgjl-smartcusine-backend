spring.application.name=smartcusine

spring.jpa.open-in-view=false

# ===============================
# DATABASE (Render)
# ===============================
spring.datasource.url=jdbc:postgresql://dpg-d7ag3oshg0os73b2v5jg-a.oregon-postgres.render.com:5432/abcgjl_smartcusine_backend?sslmode=require&connectTimeout=10&socketTimeout=30
spring.datasource.username=abcgjl_smartcusine_backend
spring.datasource.password=gx4D8ehyJNlDzGqcCNE49zrp7VD9iMxR
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none

# ===============================
# FLYWAY
# ===============================
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# ===============================
# CONFIG APP
# ===============================
app.bootstrap.admin.enabled=${APP_BOOTSTRAP_ADMIN_ENABLED:false}
app.bootstrap.admin.email=${APP_BOOTSTRAP_ADMIN_EMAIL:}
app.bootstrap.admin.password=${APP_BOOTSTRAP_ADMIN_PASSWORD:}
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS}

# ===============================

security.jwt.secret=mysupersecretkeymysupersecretkey123456

# PORT (Render)
# ===============================
server.port=${PORT:8080}